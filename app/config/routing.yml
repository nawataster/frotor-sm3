#app:
#    resource: '@AppBundle/Controller/'
#    type: annotation

# the "app_index" route name is not important yet
showindex:
    path: /{num}
    defaults: { _controller: AppBundle:Index:index, num: 1 }
    requirements:
        num: '\d+'
        
showdummy:
    path: /dummy
    defaults: { _controller: AppBundle:Index:dummy }